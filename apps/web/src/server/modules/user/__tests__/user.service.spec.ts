import { beforeEach, describe } from 'vitest'

describe('user.service', () => {
  beforeEach(async () => {
    // TODO: reset the relevant tables before each test
    // await resetTables(['User', 'Account'])
  })

  // TODO: Add tests for your user.service methods
})
